import React from 'react'
import withLogic from './withLogic' 

const Simulator = ({table, disk}) => {
  // console.log(table)
  // console.log(disk)
  return(
    <div/>
  )
}

export default withLogic(Simulator)
